'use strict'

let listNumRoman = ['I', 'II', 'III', 'IV', 'V', 'VI', 'VII', 'VIII', 'IX', 'X']

for (let i = 0; i < listNumRoman.length; i++) {
    console.log(`Num Romano de ${i+1} es ${listNumRoman[i]}`)
}
